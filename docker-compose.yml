version: '3.8'
services:
  sigint-sentinel:
    build: .
    ports:
      - '5000:5000'
    environment:
      - NEBIUS_API_KEY=${NEBIUS_API_KEY-}
    volumes:
      - .:/app
    tty: true
    stdin_open: true
